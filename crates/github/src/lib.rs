//! GitHub App integration (M4): app-manifest registration flow, installation
//! token minting (via `octocrab`, added with the implementation), and webhook
//! verification.

pub mod app;
pub mod webhook;
